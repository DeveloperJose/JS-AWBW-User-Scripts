interface DivAnimationData {
  /**
   * @property {then.dx} - Number of pixels to move left/right (column) at each step
   * @property {then.dy} - Number of pixels to move up/down (row) at each step
   * @property {then.steps} - Number of steps to take
   */
  then: [number, number, number];
}

interface MusicPlayerSettings {}
